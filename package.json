{
  "name": "text-diff-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-extension-files.mjs",
    "icons": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "monaco-editor": "^0.43.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
